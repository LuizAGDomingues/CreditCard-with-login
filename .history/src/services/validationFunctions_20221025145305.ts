interface validateFormProps {
  accountName: string;
  email: string;
  password: string;
  confirmPassword: string;
}

export function validateForm({accountName, email, password, confirmPassword}: ){
  
}